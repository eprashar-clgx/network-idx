"""Transformed features for the demographic family. The demographic source, NeighborhoodScout, is already reported at the census-tract grain, so this family needs no reshaping, pivoting, or interpolation to reach its native grain. This layer is therefore intentionally empty: the demographic feature definitions are computed directly from the tract-native source in the engineered layer."""
